import * as centered from "./centered";
import * as splitLeft from "./splitLeft";
import * as fullBleed from "./fullBleed";
import * as gradient from "./gradient";
import * as minimal from "./minimal";
import * as splitRight from "./splitRight";
import * as diagonal from "./diagonal";

export const heroTemplates = {
  hero_centered: centered,
  hero_split_left: splitLeft,
  hero_full_bleed: fullBleed,
  hero_gradient: gradient,
  hero_minimal: minimal,
  hero_split_right: splitRight,
  hero_diagonal: diagonal,
};
